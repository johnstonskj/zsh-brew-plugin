# shellcheck shell=bash disable=SC2296,SC2155,SC2086 # -*- mode: sh; eval: (sh-set-shell "zsh") -*-

emulate -L zsh

typeset name="${1}"
typeset path=$(homebrew_formula_prefix ${name})

if [[ -d "${path}" ]]; then
    typeset -x "${(U)name/-/_/}_PREFIX"="${path}"
fi

unset name path
